// @flow
import { StyleSheet } from 'aphrodite';
import { Colors } from '../../../../theme';

export default StyleSheet.create({
  wrapper: {
    color: Colors.white
  },
  text: {
    fontSize: '14px'
  },
  bold: {
    fontWeight: 'bold'
  }
});
