import { StyleSheet } from 'aphrodite';
import { Colors } from '../../../../theme';

export default StyleSheet.create({
  wrapper: {
    height: '100%',
    width: '100%',
    padding: '30px'
  },
  title: {
    color: 'white',
    textAlign: 'left',
    fontWeight: 'bold'
  },
  description: {
    color: 'white',
    textAlign: 'left',
    marginTop: 10
  }
  // old start
});
